apiVersion: v1
kind: Service
metadata:
  name: claude-halla
  namespace: claude-halla
  labels:
    app: claude-halla
spec:
  selector:
    app: claude-halla
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
